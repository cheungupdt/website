<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% if title %}{{ title }} - {% endif %}{{ site.title }}</title>
    <meta name="description" content="{{ description or site.description }}">
    <meta name="author" content="{{ site.author }}">
    <meta property="og:title" content="{{ title }}">
    <meta property="og:description" content="{{ description or site.description }}">
    <meta property="og:type" content="article">
    <meta property="og:url" content="{{ page.url }}">
    <meta name="twitter:card" content="summary_large_image">
    <link rel="stylesheet" href="/assets/css/main.css">
</head>
<body>
    <a href="#main" class="skip-link">Skip to main content</a>
    
    <header class="header">
        <div class="container">
            <div class="header-content">
                <a href="/" class="logo">{{ site.title }}</a>
                <nav class="nav">
                    <a href="/" class="nav-link">Home</a>
                    <a href="/blog/" class="nav-link active">Blog</a>
                    <a href="/projects/" class="nav-link">Projects</a>
                    <a href="/contact/" class="nav-link">Contact</a>
                </nav>
            </div>
        </div>
    </header>
    
    <main id="main" class="main">
        <div class="container">
            <article class="blog-post">
                <header class="blog-header">
                    <h1>{{ title }}</h1>
                    <div class="blog-meta">
                        <time datetime="{{ date }}">{{ date | date("MMMM DD, YYYY") }}</time>
                        {% if tags %}
                        <span class="tags">
                            {% for tag in tags %}
                            <a href="/blog/tags/{{ tag }}/" class="tag">#{{ tag }}</a>
                            {% endfor %}
                        </span>
                        {% endif %}
                    </div>
                </header>
                
                <div class="blog-content">
                    {{ content }}
                </div>
                
                <footer class="blog-footer">
                    <div class="blog-navigation">
                        {% if previousPost %}
                        <a href="{{ previousPost.url }}" class="nav-link prev">? {{ previousPost.data.title }}</a>
                        {% endif %}
                        {% if nextPost %}
                        <a href="{{ nextPost.url }}" class="nav-link next">{{ nextPost.data.title }} ?</a>
                        {% endif %}
                    </div>
                </footer>
            </article>
        </div>
    </main>
    
    <footer class="footer">
        <div class="container">
            <div class="footer-content">
                <p>&copy; {{ "now" | date("YYYY") }} {{ site.author }}. All rights reserved.</p>
                <div class="footer-links">
                    {% if social.linkedin %}
                    <a href="{{ social.linkedin }}" class="footer-link">LinkedIn</a>
                    {% endif %}
                    {% if social.github %}
                    <a href="{{ social.github }}" class="footer-link">GitHub</a>
                    {% endif %}
                    {% if social.twitter %}
                    <a href="{{ social.twitter }}" class="footer-link">Twitter</a>
                    {% endif %}
                </div>
            </div>
        </div>
    </footer>
</body>
</html>
